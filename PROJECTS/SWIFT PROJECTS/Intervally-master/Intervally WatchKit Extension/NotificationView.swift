//co2kr
import SwiftUI

struct NotificationView: View {
    var body: some View {
        Text("We are ready to go now!🙌").padding().foregroundColor(.red)
    }
}

struct NotificationView_Previews: PreviewProvider {
    static var previews: some View {
        NotificationView()
    }
}
