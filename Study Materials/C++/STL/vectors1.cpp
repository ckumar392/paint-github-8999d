// CPP program to initialize a vector like
// an array.
#include <iostream>
#include <vector>
using namespace std;

int main()
{
	vector<int> vect(10, 20, 30); //this works on online compilers

	for (int x : vect)
		cout << x << " ";
    vec.pop_back();
	return 0;
}
